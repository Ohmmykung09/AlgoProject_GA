# Java Algorithm Project

# Window

```
javac -d bin -sourcepath src src/*.java
```

```
java -cp bin MainGUI
```

# Linux / macOS:

```
mkdir -p bin && javac -d bin -sourcepath src src/Main.java src/Algorithm/*.java && java -cp bin Main
```
